machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t experimentalplatform/app-manager:${CIRCLE_BRANCH} .

test:
  override:
    - echo "TODO: Insert some tests here?"
#    - docker run -d -p 9200:9200 circleci/elasticsearch; sleep 10
#    - curl --retry 10 --retry-delay 5 -v http://localhost:9200

deployment:
  ignore:
    branch: master
    commands:
      - echo -e "\n\nWe're not uploading master anywhere."
  deploy:
    branch: /*/
    commands:
      - docker login -e ${DOCKER_EMAIL} -u ${DOCKER_USER} -p ${DOCKER_PASS}
      - docker push experimentalplatform/app-manager:${CIRCLE_BRANCH}